GOOGLE_API_KEY=
PORT=
# Optional: require login only when NODE_ENV=production (Render, etc.). Ignored for local dev / docker-compose.
ADMIN_PASSWORD=
# Optional: secret for session cookies (defaults to a dev value)
SESSION_SECRET=

# Telegram Bot (for job alerts)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Apify (for job scraping)
APIFY_API_TOKEN=