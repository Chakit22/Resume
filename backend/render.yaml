services:
  - type: web
    name: resume-tailor
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: "3000"
